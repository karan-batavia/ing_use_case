# ing_use_case

Temporary, for the team.

First, ensure you have the necessary dependencies installed. You can do this by running:

```
pip install -r requirements.txt
```

Run the following command to start the Streamlit app (from the root directory):
``` 
./docker-start.sh start;
```
